apply plugin: 'com.android.application'

android {
    compileSdkVersion 35

    defaultConfig {
        applicationId 'com.rama.mako_forever'
        minSdkVersion 9
        targetSdkVersion 35

        versionCode 1
        versionName '1.0'
    }

    externalNativeBuild {
        cmake {
            path 'CMakeLists.txt'
        }
    }
}